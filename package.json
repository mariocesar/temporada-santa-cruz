{
  "name": "temporada-santa-cruz",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json && tsc -p tsconfig.scripts.json",
    "test": "vitest run",
    "data:validate": "bun scripts/data/validate.ts",
    "data:normalize": "bun scripts/data/normalize.ts",
    "data:derive": "bun scripts/data/derive.ts",
    "data:build": "bun scripts/data/build.ts",
    "data": "bun run data:validate && bun run data:normalize && bun run data:derive && bun run data:build",
    "seed:demo": "bun scripts/data/seed/generate-demo.ts"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.13.3",
    "svelte": "^5.57.0",
    "svelte-check": "^4.7.6",
    "typescript": "~6.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0",
    "zod": "^4.6.4"
  }
}
